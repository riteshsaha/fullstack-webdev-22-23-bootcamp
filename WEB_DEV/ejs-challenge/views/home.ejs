<%- include("partials/header") %>
<h1>Home</h1>
<p><%= content %></p>
<% posts.forEach(function(post) { %>
<h1><%= post.title %></h1>
<p>
  <%= post.content.substring(0, 100) + " ..." %>
  <a href="/posts/<%= post.title %>">Read More</a>
</p>
<% }) %> <%- include("partials/footer") %> db.products.insertOne( { _id: 2,
name: "Pencil", price: 0.80, stock: 12, reviews : [ { authorName: "Roy", rating:
5, review: "Lasts longer than others." }, { authorName: "Johnny", rating: 4,
review: "Good Pencil." } ] } )
